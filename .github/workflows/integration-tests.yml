name: Morebits integration tests

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: npm install
      - name: Setup MediaWiki using Docker
        run: npm run test:integration:setup
      - run: npm run test:integration
